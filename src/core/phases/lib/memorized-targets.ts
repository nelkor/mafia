import { MemorizedTargets } from '../types'

export const createEmptyMemorizedTargets = (): MemorizedTargets => ({
  bodyguard: null,
  poisoner: null,
  ricochet: null,
  doctor: null,
  masker: null,
})
